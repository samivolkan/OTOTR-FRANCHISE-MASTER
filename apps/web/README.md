# OTOTR Public Web

Active public website workspace:

`apps/web/public-prototype`

Current active page:

`apps/web/public-prototype/ototr-web.html`

Production preparation notes:

- Source workspace is `apps/web/public-prototype`.
- Clean production upload package is `apps/web/dist-public-web`.
- Upload package notes are in `apps/web/DIST_PUBLIC_WEB_README.md`.
- Public form backend is Supabase staging Edge Function `public-api`.
- Staging API notes are in `docs/public-web-supabase-staging.md`.
- `index.html` exists only to support root URL publishing and forwards to the active page.
- See `apps/web/public-prototype/DEPLOYMENT_CHECKLIST.md` before publishing.
- Do not publish `ototr-android-firebase-test.html` or `web2.html` as public entry points.

All public website, landing page, franchise application page, SEO, responsive web, and public deployment preparation updates should be made here.

Reference copies under `apps/admin/prototype` are preserved for admin/prototype history and should not be edited for public website work unless a separate admin task explicitly requires it.
