# Declarative Schemas

Reserved for future Supabase declarative schema files.

Current decision:

- The project is starting with reviewed SQL migrations under `../migrations`.
- Do not add schema files here until the team explicitly chooses a declarative schema workflow.

